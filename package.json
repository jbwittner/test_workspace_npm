{
  "name": "my-workspaces-powered-project",
  "private": "true",
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start -w infra",
    "clean": "npm run clean --workspaces",
    "test": "npm run test --workspaces",
    "lint": "eslint .",
    "prettier": "prettier --check . ",
    "prettier:fix": "prettier --write ."
  },
  "workspaces": [
    "domain",
    "infra"
  ],
  "devDependencies": {
    "eslint": "^8.56.0",
    "@types/node": "^20.10.5",
    "prettier": "^3.1.1"
  }
}
